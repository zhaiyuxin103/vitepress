1. Locate your project entry file, typically `index.html`.

2. Add `user-scalable=no` to the `<meta name="viewport" />` tag, for example:

   ```html
   <meta
     name="viewport"
     content="width=device-width,initial-scale=1.0,user-scalable=no"
   />
   ```
